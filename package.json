{
  "name": "mizan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @mizan/web dev",
    "build": "pnpm --filter @mizan/web build",
    "start": "pnpm --filter @mizan/web start",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter @mizan/web db:migrate",
    "db:generate": "pnpm --filter @mizan/web db:generate"
  },
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "@parcel/watcher",
      "@tailwindcss/oxide",
      "esbuild"
    ]
  }
}
